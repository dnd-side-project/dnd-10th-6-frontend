import { Meta, Typeset, Unstyled } from '@storybook/blocks'

<Meta title="Typography" />

# Typography

export const typography = {
  type: {
    primary: 'PretendardVariable',
    weight: {
      medium: '500',
      bold: '700',
    },
    size: {
      mainTitle1: 28,
      mainTitle2: 24,
      subTitle1: 20,
      subTitle2: 18,
      body1: 16,
      body2: 15,
      body3: 14,
      caption1: 13,
      caption2: 12,
    },
  },
}

export const SampleText = 'Namui Wiki Text'

**Primary Font** : PretendardVariable
**Weighs** : Medium 500

### tag 사용 Rule

<Unstyled>
    <div className="grid gap-xs">
        <h1>Main Title 1 은 h1 태그를 사용합니다.</h1>
        <h2>Main Title 2은 h2 태그를 사용합니다.</h2>
        <h3>Sub Title 1은 h3 태그를 사용합니다.</h3>
        <h3>Sub Title 2은 h3 태그를 사용합니다.</h3>
        <p>Body 1은 p 태그를 사용합니다.</p>
        <p>Body 2은 p 태그를 사용합니다.</p>
        <p>Body 3은 p 태그를 사용합니다.</p>
        <p>Caption 1은 p 태그를 사용합니다.</p>
        <p>Caption 2은 p 태그를 사용합니다.</p>
</div>
</Unstyled>

<Typeset
  fontSizes={[
    Number(typography.type.size.mainTitle1),
    Number(typography.type.size.mainTitle2),
    Number(typography.type.size.subTitle1),
    Number(typography.type.size.subTitle2),
    Number(typography.type.size.body1),
    Number(typography.type.size.body2),
    Number(typography.type.size.body3),
    Number(typography.type.size.caption1),
    Number(typography.type.size.caption2),
  ]}
  fontWeight={typography.type.weight.medium}
  sampleText={SampleText}
  fontFamily={typography.type.primary}
/>

**Primary Font** : PretendardVariable
**Weighs** : Bold 700

<Typeset
  fontSizes={[
    Number(typography.type.size.mainTitle1),
    Number(typography.type.size.mainTitle2),
    Number(typography.type.size.subTitle1),
    Number(typography.type.size.subTitle2),
    Number(typography.type.size.body1),
    Number(typography.type.size.body2),
    Number(typography.type.size.body3),
    Number(typography.type.size.caption1),
    Number(typography.type.size.caption2),
  ]}
  fontWeight={typography.type.weight.bold}
  sampleText={SampleText}
  fontFamily={typography.type.primary}
/>
